#include<stdio.h>
#include<conio.h>
#include<string.h>

int main (void) {
    char arry[100];
    int i,count=0;
    float a,b;
    printf("Enter a 2 Number ");
    scanf("%d %d",&a,&b);
    if(a,b == 0){
        printf("its divisible");
        } 
    else {
        printf("its not divisible"); 
        }
    getch();
    return 1;
}